# App Examples

Small JellyFrame applications used to validate the app package model and common
embedded UI workflows.

These examples should stay close to what a wearable or small embedded product
can reasonably run: local HTML/CSS/classic JS, bounded assets and no remote page
loading.
