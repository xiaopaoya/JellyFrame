# Watch Weather Example

A compact weather-style app used to test package structure, local resources,
text rendering and small-screen layout.

The example is intentionally local-first. Network-backed weather data belongs in
host/runtime APIs, not in the core renderer.
