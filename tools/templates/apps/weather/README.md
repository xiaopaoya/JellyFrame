# Weather Template

Weather app template for validating card-like data layout and future host
network integration.

The current template should remain usable with local/static data because remote
page loading is intentionally not part of the embedded core.
