# Jelly Controls

> 最后更新：2026-07-07；适用版本：0.5.0-dev

展示 JellyFrame 胶体界面系统的小型可安装 package。

- 使用标准 HTML 控件和少量 JavaScript。
- 动效只落在 `opacity`、`background-color` 和 `transform`。
- 不依赖 canvas、filter、backdrop-filter 或 layout 动画。
