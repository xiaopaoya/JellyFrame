# Watch Weather Example

A compact watch-weather source package used to validate package structure, local
resources, event delegation, small-screen grid layout and Win32/pseudo-browser
preview parity.

The example is local-first. Runtime network-backed weather data should enter
through future host APIs declared by package capabilities, not through remote
page loading in the renderer.
