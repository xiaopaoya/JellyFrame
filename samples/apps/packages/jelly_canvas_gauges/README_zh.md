# Jelly Canvas Gauges

> 最后更新：2026-07-07；适用版本：0.5.0-dev

面向可穿戴仪表盘和紧凑数据图形的 Canvas 2D V0.3 示例。布局、文本和控件仍由 DOM/CSS
负责；Canvas 只用于环形仪表、标签和很难用盒模型表达的小图表。

该 package 声明 `graphics.canvas2d`。目标平台需要通过 `hostServices.canvas2d`
显式开启支持。
