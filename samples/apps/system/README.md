# System App Samples

This directory contains privileged app samples used by desktop hosts and bring-up
tests. They exercise the same JellyFrame render/runtime pipeline as normal apps,
but the host decides whether to grant system capabilities.
